# BUILD_TICKETS.md

AUTOMATION_STATUS: IN_PROGRESS

Ticket statuses:

- TODO
- IN_PROGRESS
- DONE
- BLOCKED

The build loop must select the lowest-numbered TODO or IN_PROGRESS ticket.

---

## 000 — Bootstrap repository skeleton

Status: DONE

Create the initial repository structure.

Required:

- `README.md`
- `pyproject.toml`
- `uv.lock` if generated by `uv`
- `src/job_runner_platform/`
- `tests/`
- `docs/`
- `docs/decisions/`
- `scripts/`
- `.gitignore`
- `example.env`
- `Makefile` or `justfile`
- basic package import test

Use:

- Python 3.12
- uv
- hatchling
- Ruff
- mypy strict mode
- pytest
- pytest-cov

The README must clearly state:

- independent public portfolio project
- public-safety constraints
- no arbitrary command execution
- intended backend/platform skills demonstrated

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 001 — Add FastAPI app shell, settings, logging, and health endpoint

Status: DONE

Implement:

- FastAPI app factory
- environment-backed settings with `JOB_RUNNER_` prefix
- docs disabled by default
- structured JSON logging
- `X-Request-ID` propagation
- `GET /healthz`

Add tests for:

- health endpoint
- request ID propagation
- docs disabled by default
- docs enabled when configured

Update README configuration section.

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 002 — Define job domain model and API schemas

Status: DONE

Implement domain definitions for:

- job ID
- job type
- job status
- job priority if useful
- attempts
- max attempts
- payload
- result
- error message
- idempotency key
- timestamps
- lease owner
- lease expiry

Use explicit statuses:

- queued
- running
- succeeded
- failed
- cancel_requested
- cancelled
- dead_lettered

Add Pydantic schemas for:

- create job request
- create job response
- job detail response
- job list response
- cancellation response

Restrict job types to safe allowlisted handlers.

Add tests for schema validation and invalid job types.

Update docs if needed.

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 003 — Add PostgreSQL persistence and Alembic migrations

Status: DONE

Implement:

- async SQLAlchemy engine/session setup
- database models
- Alembic configuration
- initial migration
- repository-friendly database package structure

The `jobs` table should include:

- UUID primary key
- job_type
- status
- payload JSON
- result JSON nullable
- error_message nullable
- attempts
- max_attempts
- idempotency_key nullable
- lease_owner nullable
- lease_expires_at nullable
- created_at
- updated_at
- queued_at
- started_at nullable
- finished_at nullable

Add useful indexes, including:

- status
- created_at
- idempotency_key where appropriate
- lease_expires_at where appropriate

Add tests for migrations or database model creation.

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 004 — Add repository layer

Status: DONE

Implement repository methods for:

- create job
- get job by ID
- list jobs with pagination/status filtering
- find by idempotency key
- request cancellation
- claim queued job
- complete job
- fail job
- mark dead-lettered
- requeue job
- find stale leased jobs

Keep SQL inside repositories.

Add tests using PostgreSQL integration where practical.

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 005 — Add Redis queue abstraction

Status: DONE

Implement a queue abstraction that hides Redis details.

PostgreSQL remains the source of truth. Redis is used as a dispatch signal.

Required behaviours:

- enqueue job ID
- dequeue or poll next job ID
- acknowledge or safely ignore duplicate messages
- tolerate duplicate messages by checking database state
- expose readiness check

Use a simple reliable design. Avoid overengineering.

Add fake queue implementation for tests if useful.

Add tests for enqueue/dequeue behaviour and duplicate message safety.

Document the design in `docs/decisions/0002-redis-as-dispatch-signal.md`.

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 006 — Add job service for create, get, list, and cancellation

Status: DONE

Implement service layer methods:

- create job
- get job
- list jobs
- cancel job

Create job should:

- validate allowlisted job type
- persist job in PostgreSQL
- honour optional idempotency key
- enqueue job ID to Redis
- return existing job on idempotent replay where appropriate

Cancel job should:

- mark queued jobs as cancelled or cancel_requested
- mark running jobs as cancel_requested
- reject cancellation for terminal states with clear behaviour

Add tests for:

- create
- idempotency replay
- invalid job type
- list pagination
- cancel queued job
- cancel terminal job

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 007 — Add API routes for jobs

Status: DONE

Implement routes:

- `POST /jobs`
- `GET /jobs`
- `GET /jobs/{job_id}`
- `POST /jobs/{job_id}/cancel`

Support:

- JSON request/response models
- pagination
- optional status filter
- clear 404s
- clear 409s where appropriate

Add API tests.

Update README API surface.

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 008 — Add safe allowlisted job handlers

Status: DONE

Implement job handlers:

- `echo`
- `sleep`
- `checksum`
- `fail_once`
- `always_fail`

Rules:

- no arbitrary shell commands
- no user-supplied code execution
- no subprocess execution
- no host filesystem mutation beyond normal app logs/state
- deterministic behaviour where practical
- max sleep duration enforced

Add unit tests for each handler.

Document handler payload shapes.

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 009 — Add worker runtime

Status: DONE

Implement a worker process/CLI that:

- has a worker ID
- polls Redis for job IDs
- claims queued jobs through repository/service logic
- runs allowlisted handlers
- records success/failure
- logs job lifecycle events
- handles duplicate queue messages safely
- exits cleanly on shutdown signal where practical

Add tests for worker success path.

Add local run instructions.

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 010 — Add retry and dead-letter behaviour

Status: DONE

Implement retry policy:

- failed jobs retry until max attempts
- retryable failures requeue the job
- jobs exceeding max attempts become dead_lettered
- errors are recorded safely
- attempts are incremented consistently

Add tests for:

- fail once then succeed
- always fail then dead-letter
- max attempts
- error recording

Update runbook.

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 011 — Add leases and stale job recovery

Status: DONE

Implement lease behaviour:

- worker claims job with lease owner and lease expiry
- running jobs whose lease expires can be recovered
- stale jobs are requeued or dead-lettered according to attempts/policy
- recovery operation is explicit and testable

Add service method and/or worker loop support for stale recovery.

Add tests for stale lease recovery.

Document in `docs/decisions/0004-leases-and-stale-job-recovery.md`.

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 012 — Add cancellation behaviour in worker

Status: TODO

Implement cancellation checks:

- queued job cancellation
- running job cancellation request
- cooperative cancellation where safe
- cancelled terminal state
- logs and metrics for cancellation

The `sleep` handler should be able to observe cancellation between short sleep intervals.

Add tests for cancellation.

Update docs.

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 013 — Add readiness and dependency checks

Status: TODO

Implement:

- `GET /readyz`
- PostgreSQL readiness check
- Redis readiness check

Readiness should fail clearly when dependencies are unavailable.

Add tests with fakes/mocks where practical.

Update README.

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 014 — Add Prometheus metrics

Status: TODO

Implement:

- `GET /metrics`
- API request metrics if practical
- job lifecycle counters
- job duration histogram
- queue polling/worker metrics if practical

Metrics should include:

- jobs_created_total
- jobs_started_total
- jobs_succeeded_total
- jobs_failed_total
- jobs_retried_total
- jobs_dead_lettered_total
- jobs_cancelled_total
- job_duration_seconds

Add tests for metrics endpoint and key metric names.

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 015 — Add optional API key authentication

Status: TODO

Implement optional API key auth for business endpoints.

Rules:

- disabled by default for local exploration
- enabled with `JOB_RUNNER_AUTH_ENABLED=true`
- configured with `JOB_RUNNER_AUTH_API_KEYS`
- use `X-API-Key`
- do not protect `/healthz`, `/readyz`, or `/metrics`

Add tests for:

- auth disabled
- missing API key
- invalid API key
- valid API key
- health/readiness/metrics unaffected

Update README.

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 016 — Add Dockerfile and Docker Compose stack

Status: TODO

Implement:

- Dockerfile
- non-root runtime user
- container health check
- docker-compose.yml

Compose services:

- api
- worker
- postgres
- redis
- prometheus
- grafana

Add local placeholder environment only.

Add README quick start.

Validate with `docker compose config`.

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 017 — Add Prometheus and Grafana configuration

Status: TODO

Implement:

- Prometheus scrape config for API and worker if applicable
- Grafana provisioning
- basic dashboard JSON
- docs explaining local observability URLs

Update Docker Compose if needed.

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 018 — Add GitHub Actions CI

Status: TODO

Implement `.github/workflows/ci.yml`.

CI should run:

- shell syntax checks for scripts
- public-safety/private-term guardrail if present
- architecture/layering guardrail if present
- `uv sync --locked --all-groups`
- Ruff check
- Ruff format check
- mypy
- Docker Compose config validation
- Alembic migration upgrade
- pytest with coverage

Use PostgreSQL service container where needed.

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 019 — Add automation guardrails

Status: TODO

Add scripts similar in spirit to:

- public-safety/private-term check
- architecture/layering check

The public-safety check should catch obvious risky placeholders such as:

- real-looking secrets
- internal hostnames if configured
- forbidden employer-specific terms if present
- accidental `.env` secrets

The layering check should catch obvious route-to-database or route-to-Redis violations where possible.

Wire scripts into quality gate and CI.

Add tests for guardrail scripts if practical.

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 020 — Add docs: architecture, operations, runbook, API walkthrough

Status: TODO

Create or complete:

- `docs/architecture.md`
- `docs/api-walkthrough.md`
- `docs/operations.md`
- `docs/runbook.md`

Docs should explain:

- architecture
- job lifecycle
- state transitions
- queue design
- retry/dead-letter behaviour
- cancellation
- leases
- metrics
- local operation
- failure modes
- known limitations

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 021 — Add ADRs

Status: TODO

Create ADRs:

- `docs/decisions/0001-postgres-source-of-truth.md`
- `docs/decisions/0002-redis-as-dispatch-signal.md`
- `docs/decisions/0003-allowlisted-demo-job-handlers.md`
- `docs/decisions/0004-leases-and-stale-job-recovery.md`

Each ADR should include:

- status
- context
- decision
- consequences

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 022 — Add smoke/demo scripts

Status: TODO

Add safe local demo scripts or documented curl walkthroughs showing:

- create echo job
- create checksum job
- create fail_once job
- observe retry
- create always_fail job
- observe dead-letter
- cancel sleep job
- check metrics

Scripts must not require secrets or external services beyond local Docker Compose.

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 023 — Final README polish

Status: TODO

Polish README so the first screen clearly sells the portfolio value.

README must include:

- concise headline
- public-safety constraints
- implemented scope
- out-of-scope section
- requirements
- quick start
- configuration table
- API surface
- worker instructions
- observability section
- testing/quality gates
- architecture links
- limitations
- portfolio framing

Run `scripts/quality-gate.sh`.

Commit when complete.

---

## 024 — Final autonomous review and completion marker

Status: TODO

Perform a final repository review.

Check:

- no arbitrary command execution
- no employer/private details
- no secrets
- all docs are consistent
- README matches implemented behaviour
- CI exists and is coherent
- Docker Compose validates
- tests pass
- architecture boundaries are respected
- public portfolio framing is clear

Run full quality gate.

If everything is complete, set:

```text
AUTOMATION_STATUS: DONE
```

Commit final review.
